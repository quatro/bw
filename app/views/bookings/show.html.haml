
=render 'application/pageheader', title: @model.title

%section.content
  .container-fluid
    / Small boxes (Stat box)
    .row
      .col-lg-3.col-6
        / small box
        .small-box.bg-info
          .inner
            %h3 #{number_to_currency(@model.total)}
            %p Total
          .icon
            %i.ion.ion-bag
          -#%a.small-box-footer{:href => "#"}
            More info
            %i.fas.fa-arrow-circle-right

      .col-lg-3.col-6
        / small box
        .small-box.bg-warning
          .inner
            %h3 #{@model.hotel.name}
            %p Hotel
          .icon
            %i.ion.ion-bag
          %a.small-box-footer{href: tenant_hotel_path(@model.tenant, @model.hotel)}
            More info
            %i.fas.fa-arrow-circle-right


%hr
=render 'application/showview', model: @model
