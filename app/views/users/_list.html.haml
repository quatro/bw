
%table.data-table-requests.stripe
  %thead
    %tr
      %th First
      %th Last
      %th Email
      %th Serviced Bookings
      %th

  %tbody
    - models.each do |u|
      %tr
        %td #{u.first}
        %td #{u.last}
        %td #{u.email}
        %td #{u.assigned_bookings.count}
        %td
          =link_to "Show", user_path(u)
          =link_to "Edit", edit_user_path(u)