- f = !local_assigns[:f].nil? ? f : local_assigns[:form]
.nested-fields
  = f.input :guest1_name, input_html: { class: 'guest1_name'}
  = f.input :guest2_name, input_html: { class: 'guest2_name'}
  = link_to_remove_association "Remove Room", f
  %hr
